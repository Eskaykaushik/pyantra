[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "pyantra-eval"
version = "0.3.0"
description = "Trajectory evaluation, LLM judges, and a pytest plugin for Pyantra."
readme = "README.md"
requires-python = ">=3.10"
license = "MIT"
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["pyantra_eval"]
