[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "pyantra-memory"
version = "0.5.1"
description = "Vector stores, RAG, and caching for Pyantra."
readme = "README.md"
requires-python = ">=3.10"
license = "MIT"
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["pyantra_memory"]
