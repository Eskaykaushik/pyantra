[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "pyantra-otel"
version = "0.3.0"
description = "OpenTelemetry and tracing exporters for Pyantra."
readme = "README.md"
requires-python = ">=3.10"
license = "MIT"
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["pyantra_otel"]
