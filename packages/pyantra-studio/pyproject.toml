[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "pyantra-studio"
version = "0.5.1"
description = "Local development server and visual web debugger for Pyantra."
readme = "README.md"
requires-python = ">=3.10"
license = "MIT"
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["pyantra_studio"]
